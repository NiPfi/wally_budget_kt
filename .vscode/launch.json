{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run WallyBudget on Running Emulator",
            "type": "node-terminal",
            "request": "launch",
            "command": "bash .vscode/run-android-emulator.sh",
            "cwd": "${workspaceFolder}"
        }
    ]
}
